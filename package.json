{
  "name": "mercredistes-liste-photos",
  "private": true,
  "scripts": {
    "start": "hugo server -w --enableGitInfo",
    "build": "hugo --minify --enableGitInfo",
    "build:functions": "netlify-lambda build assets/lambda",
    "server": "hugo server"
  },
  "dependencies": {
    "autoprefixer": "10.4.8",
    "postcss": "8.4.31",
    "postcss-cli": "10.0.0"
  }
}
